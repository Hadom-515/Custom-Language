func main() {
    int[4] array;

    # Should print out '2'
    array[0] = 2;
    prt(array[0]);

    # Should print out '4'
    array [1] = array[0] + array[0];
    prt(array[1]);

    # Should print out '8'
    array[2] = array[1] + 2 * 2;
    prt(array[2]);
 
    
}
